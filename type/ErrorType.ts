export enum ErrorAddTransaction {
  Type = "type",
  Wallet = "wallet",
  Date = "date",
  Amount = "amount",
  Description = "description",
}

export enum ErrorUpdateProfile {
  Name = "name",
  Currency = "currency",
}